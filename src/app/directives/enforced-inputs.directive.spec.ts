import { EnforcedInputsDirective } from './enforced-inputs.directive';

describe('EnforcedInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new EnforcedInputsDirective();
    expect(directive).toBeTruthy();
  });
});
